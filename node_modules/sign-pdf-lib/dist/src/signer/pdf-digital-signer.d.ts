/// <reference types="node" />
import { PdfVerifySignaturesResult, SignatureField } from "../models";
import { SignFieldParameters, AddFieldParameters, SignDigitalParameters } from "../models/parameters";
import { SignerSettings } from "../models/settings";
export declare class PdfDigitalSigner {
    #private;
    constructor(settings: SignerSettings);
    addPlaceholderAsync(pdf: Buffer, info: SignDigitalParameters): Promise<Buffer>;
    addFieldAsync(pdf: Buffer, info: AddFieldParameters): Promise<Buffer>;
    signAsync(pdf: Buffer, info: SignDigitalParameters): Promise<Buffer>;
    signFieldAsync(pdf: Buffer, info: SignFieldParameters): Promise<Buffer>;
    verifySignaturesAsync(pdf: Buffer): Promise<PdfVerifySignaturesResult | undefined>;
    getFieldsAsync(pdf: Buffer): Promise<SignatureField[]>;
    private getPlaceholderParameters;
}
