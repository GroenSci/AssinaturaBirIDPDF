/// <reference types="node" />
import { Rectangle, SignatureText } from '../models';
export interface AddVisualSignatureBackgroundParameters {
    pageIndex: number;
    rectangle: Rectangle;
    background: Buffer;
    texts?: SignatureText[];
    reverseY?: boolean;
    backgroundName?: string;
}
export interface AddVisualSignatureTextsParameters {
    pageIndex: number;
    rectangle: Rectangle;
    background?: Buffer;
    texts: SignatureText[];
    reverseY?: boolean;
    backgroundName?: string;
}
export type AddVisualSignatureParameters = AddVisualSignatureBackgroundParameters | AddVisualSignatureTextsParameters;
export declare class PdfDocumentVisualSigner {
    #private;
    static fromPdfAsync(pdf: Buffer): Promise<PdfDocumentVisualSigner>;
    private constructor();
    addVisualSignatureAsync({ pageIndex, rectangle, texts, background, backgroundName, reverseY }: AddVisualSignatureParameters): Promise<void>;
    saveAsync(): Promise<Buffer>;
}
