/// <reference types="node" />
import { SignVisualParameters } from "../models/parameters";
export declare class PdfVisualSigner {
    signAsync(pdf: Buffer, { pageNumber, rectangle, ...visual }: SignVisualParameters): Promise<Buffer>;
}
