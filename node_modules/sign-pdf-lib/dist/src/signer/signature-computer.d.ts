/// <reference types="node" />
import { SignatureComputerSettings } from '../models/settings/signature-computer-settings';
export declare class SignatureComputer {
    #private;
    constructor(settings: SignatureComputerSettings);
    computeSignature(signBuffer: Buffer, date: Date): Buffer;
}
